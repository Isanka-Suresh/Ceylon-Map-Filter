# Google Cloud Configuration
# Obtained from Google Cloud Console -> APIs & Services -> Credentials -> API Keys
# Ensure the key has permissions for: Geocoding API and Places API (New)
# Required for the interactive map view in the browser
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="your-google-maps-api-key"
